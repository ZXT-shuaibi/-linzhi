# 认证模块开发实现清单（可直接开工）

> 适用范围：邻里知光重构期认证域（模块1）
> 事实来源：`openapi.yaml` + `邻里知光-技术分档-详细版.md`
> 目标：让后端工程师按本清单直接实现/重构 `Auth` 模块

## 1. 接口总览（固定）

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/token/refresh`
- `POST /api/v1/auth/logout`
- `POST /api/v1/auth/password/reset`

响应统一使用：`ApiResponse` / `ErrorResponse`。

## 2. 数据契约（DTO）

### 2.1 入参 DTO

1. `RegisterRequest`
- `phone: string`（必填）
- `password: string`（必填）
- `nickname: string`（必填）
- `smsCode: string`（必填）

2. `LoginRequest`
- `identifier: string`（必填，手机号或账号）
- `password: string`（必填）
- `channel: string`（可选，APP/H5/MP）
- `captchaToken: string`（可选，触发风控时必填）

3. `RefreshTokenRequest`
- `refreshToken: string`（必填）

4. `LogoutRequest`
- `refreshToken: string`（必填）
- `logoutScope: enum(current_device, all_devices)`（可选，默认 `current_device`）

5. `PasswordResetRequest`
- `phone: string`（必填）
- `smsCode: string`（必填）
- `newPassword: string`（必填）

### 2.2 出参 DTO

1. `AuthTokens`
- `accessToken: string`
- `accessExpiresAt: datetime`
- `refreshToken: string`
- `refreshExpiresAt: datetime`
- `tokenType: string`（固定 `Bearer`）

2. `AuthSessionData`
- `userId: string`
- `tokens: AuthTokens`

3. `ActionResult`
- `success: boolean`
- `action: string`
- `resourceId: string`
- `status: string`

## 3. Controller 结构（建议固定）

建议类：`AuthController`，前缀：`/api/v1/auth`

- `register(RegisterRequest req)`
- `login(LoginRequest req)`
- `refreshToken(RefreshTokenRequest req)`
- `logout(LogoutRequest req, Principal principal)`
- `resetPassword(PasswordResetRequest req)`

Controller 只做：参数校验 + 调用 service + 返回统一信封。

## 4. Service 分层与职责

### 4.1 服务拆分

- `AuthService`
  - 注册、登录、登出、重置密码编排
- `JwtService`
  - Access/Refresh 签发、解析、验签（RS256）
- `RefreshTokenStore`
  - Redis 白名单写入/删除/批量失效
- `PasswordService`
  - 密码哈希与强度校验
- `AuthRiskService`
  - 登录失败计数、限流校验、验证码挑战判定
- `LoginAuditService`（可选）
  - 登录审计日志异步记录

### 4.2 关键事务边界

- 注册：`user` 写入成功后再签发 token；失败则回滚注册。
- 登录：主流程不依赖异步审计；审计失败不影响登录成功。
- 刷新：旧 refresh 失效 + 新 refresh 入白名单必须同请求原子完成。
- 登出：删除当前设备 refresh（或全部设备 refresh）为核心动作。
- 重置密码：更新密码后可选强制失效全部 refresh。

## 5. 五个接口的后端处理步骤（逐步）

## 5.1 注册 `POST /auth/register`

1. 校验 `phone/password/nickname/smsCode`。
2. 校验短信验证码有效性（验证码服务）。
3. 检查手机号是否已存在（唯一索引）。
4. 密码加密存储（BCrypt/Argon2）。
5. 创建用户记录。
6. 签发 Access + Refresh。
7. `RefreshTokenStore.save(uid,jti,ttl=7d)`。
8. 返回 `AuthSessionData`。

失败分支：
- 手机号已注册 -> HTTP 409
- 验证码无效/过期 -> HTTP 400
- 限流 -> HTTP 429

## 5.2 登录 `POST /auth/login`

1. 校验 `identifier/password`。
2. 风控校验（失败次数/IP/验证码策略）。
3. 查询用户并校验密码。
4. 登录失败累加 `auth:fail:{identifier}`（短 TTL）。
5. 登录成功清理失败计数。
6. 签发 Access + Refresh。
7. 保存 refresh 白名单。
8. 异步记录登录审计日志。
9. 返回 `AuthSessionData`。

失败分支：
- 账号或密码错误 -> HTTP 401
- 参数错误 -> HTTP 400
- 限流/风控 -> HTTP 429

## 5.3 刷新 `POST /auth/token/refresh`

1. 验签 refresh token（必须 `token_type=refresh`）。
2. 从 token 提取 `uid/jti`。
3. 检查 Redis 白名单 `auth:rt:{uid}:{jti}` 是否存在。
4. 不存在则拒绝（可能重放/已注销）。
5. 生成新 Access + 新 Refresh（新 `jti`）。
6. 原子操作：删除旧 `jti` + 写入新 `jti`。
7. 返回 `AuthTokens`。

失败分支：
- token 非法/过期 -> HTTP 401
- 白名单不存在 -> HTTP 401
- 风险控制触发 -> HTTP 401 或 429（按策略）

## 5.4 登出 `POST /auth/logout`

1. 校验 `refreshToken`。
2. 解析 token 拿到 `uid/jti`。
3. 根据 `logoutScope` 执行：
- `current_device`：删除当前 `auth:rt:{uid}:{jti}`
- `all_devices`：删除 `auth:rt:{uid}:*`
4. 返回 `ActionResult(success=true, action=logout)`。

幂等要求：重复登出应返回成功，不报错。

## 5.5 重置密码 `POST /auth/password/reset`

1. 校验 `phone/smsCode/newPassword`。
2. 校验短信验证码。
3. 更新密码哈希。
4. 可选：删除用户所有 refresh 白名单（强制重新登录）。
5. 返回 `ActionResult(success=true, action=password_reset)`。

失败分支：
- 验证码无效 -> HTTP 400
- 频次超限 -> HTTP 429

## 6. Redis 设计（认证域）

### 6.1 Key 规范

1. `auth:rt:{uid}:{jti}`
- 含义：refresh 白名单项
- 值：`1` 或最小元数据 JSON
- TTL：7 天（与 refresh 过期一致）

2. `auth:fail:{identifier}`
- 含义：登录失败计数
- 值：失败次数整数
- TTL：建议 5~15 分钟

3. `auth:logout:recent:{uid}`（可选）
- 含义：最近主动登出时间
- 值：时间戳
- TTL：建议 1~7 天

### 6.2 原子操作要求

- 刷新轮换必须使用 Lua 或事务保证：`DEL oldKey` + `SETEX newKey` 同步完成。
- `all_devices` 登出可用 key 索引集合或 scan+del（推荐维护用户 refresh 索引集合）。

## 7. 安全策略（最小可用）

- Access TTL：15 分钟；Refresh TTL：7 天。
- RS256 签名，支持 `kid` 多 key 轮换。
- 密码只存哈希，不可逆。
- 登录失败阈值后触发验证码挑战。
- 异常 IP/设备切换时可提高刷新校验等级。

## 8. 错误码矩阵（建议落地）

> HTTP 状态码固定按 OpenAPI；以下业务码用于 `ErrorResponse.code`。

| 场景 | HTTP | 业务码（建议） | message |
| --- | --- | --- | --- |
| 参数校验失败 | 400 | `AUTH_400_VALIDATION` | 参数错误 |
| 短信验证码无效 | 400 | `AUTH_400_SMS_INVALID` | 验证码错误或已过期 |
| 未认证/令牌无效 | 401 | `AUTH_401_UNAUTHORIZED` | 认证失败 |
| refresh 白名单缺失 | 401 | `AUTH_401_REFRESH_REPLAY` | 刷新令牌已失效 |
| 手机号冲突 | 409 | `AUTH_409_PHONE_EXISTS` | 手机号已注册 |
| 登录/重置限流 | 429 | `AUTH_429_RATE_LIMIT` | 操作过于频繁 |

## 9. 数据库与索引建议

### 9.1 主表（最小）

- `user(id, phone, password_hash, nickname, avatar, status, create_time, update_time)`
- `login_logs(id, user_id, identifier, channel, ip, ua, status, created_at)`（可选）

### 9.2 索引

- `user.phone` 唯一索引
- `login_logs(user_id, created_at)` 普通索引
- `login_logs(identifier, created_at)` 普通索引

## 10. 观测与告警

### 10.1 指标

- `auth_login_total{result}`
- `auth_refresh_total{result}`
- `auth_logout_total{scope}`
- `auth_password_reset_total{result}`
- `auth_token_issue_rt_ms`
- `auth_refresh_rt_ms`

### 10.2 日志字段（至少）

- `requestId`
- `userId`（匿名可空）
- `identifier`
- `ip`
- `ua`
- `action`
- `result`
- `errorCode`

## 11. 测试清单（必须覆盖）

### 11.1 单元测试

- 密码哈希与比对。
- JWT 签发与验签（含过期、错误签名、错误 tokenType）。
- Refresh 白名单存取与 TTL。
- 刷新轮换原子逻辑。

### 11.2 集成测试

- 注册成功、手机号重复失败。
- 登录成功、密码错误失败、限流生效。
- 刷新成功、重复刷新失败（重放防护）。
- 登出后 refresh 立即失效。
- 重置密码后旧密码失效、可选会话全失效。

### 11.3 并发测试

- 同一 refresh 并发刷新，仅一个成功。
- 同账号高并发登录失败计数正确。
- `all_devices` 登出对多设备 token 生效。

## 12. 实施顺序（建议 2 个迭代）

### 迭代1（先跑通）

1. 5个接口 + 基础 DTO + JWT RS256。
2. Refresh 白名单 + 刷新轮换。
3. 基础限流（登录/重置）。

### 迭代2（安全加固）

1. 风控挑战（验证码门槛）。
2. 审计日志与告警。
3. `all_devices` 登出优化（索引集合）。

## 13. 与现有文档映射

- 上层概述：`后端API-超详细.md` 模块1
- 本文：实现层细化，不改变上层路径与响应契约


# 认证模块开发实现清单（可直接开工）

> 适用范围：邻里知光重构期认证域（模块1）
> 事实来源：`openapi.yaml` + `邻里知光-技术分档-详细版.md`
> 目标：让后端工程师按本清单直接实现/重构 `Auth` 模块
> 重构主文档：`认证模块-重构对照方案.md`
> 说明：本清单偏实施步骤，具体取舍以“重构对照方案”中的基线保留原则为准。
> 特别说明：本清单默认保留 `linli` 已有增强能力，不允许因对齐 `zhiguang` 而回退黑名单、失败计数、验证码挑战等安全设计。
> 非企业级约束：按课程项目/作品集项目的中等复杂度推进，不接真实短信服务，不做生产级多设备会话治理，不做过重的审计与监控设计。

## 1. 接口总览（固定）

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/token/refresh`
- `POST /api/v1/auth/logout`
- `POST /api/v1/auth/password/reset`

建议补一个最小验证码接口：

- `POST /api/v1/auth/send-code`
- 仅用于生成验证码、写入 Redis、输出日志/调试信息，不接第三方短信平台

响应统一使用：`ApiResponse` / `ErrorResponse`。

## 1.1 linli 增强能力（必须保留）

- 登录黑名单机制
- 登录失败次数跟踪
- 达阈值后强制验证码挑战
- Refresh Token 单次消费
- Snowflake 用户 ID
- 注：`all_devices` 若保留字段，可暂不实现真实语义

## 2. 数据契约（DTO）

### 2.1 入参 DTO

1. `RegisterRequest`
- `phone: string`（必填）
- `account: string`（必填，4~32位字母/数字/下划线）
- `password: string`（必填）
- `nickname: string`（必填）
- `smsCode: string`（必填）
- 说明：建议通过 mock `send-code` 获取验证码，不建议退化成固定测试码

2. `LoginRequest`
- `identifier: string`（必填，手机号或账号）
- `password: string`（必填）
- `channel: string`（可选，APP/H5/MP）
- `captchaCode: string`（可选，触发风控时必填，6位数字登录验证码）

3. `RefreshTokenRequest`
- `refreshToken: string`（必填）

4. `LogoutRequest`
- `refreshToken: string`（必填）
- `logoutScope: enum(current_device, all_devices)`（可选，当前阶段可固定按 `current_device` 处理）

5. `PasswordResetRequest`
- `phone: string`（必填）
- `smsCode: string`（必填）
- `newPassword: string`（必填）

### 2.2 出参 DTO

1. `RegisterResult`
- `userId: string`
- `phone: string`
- `account: string`
- `nextAction: string`（当前固定为 `login`）
- `status: string`（当前固定为 `registered`）

2. `AuthTokens`
- `accessToken: string`
- `accessExpiresAt: datetime`
- `refreshToken: string`
- `refreshExpiresAt: datetime`
- `tokenType: string`（固定 `Bearer`）

3. `AuthSessionData`
- `userId: string`
- `tokens: AuthTokens`

4. `ActionResult`
- `success: boolean`
- `action: string`
- `resourceId: string`
- `status: string`

## 3. Controller 结构（建议固定）

建议类：`AuthController`，前缀：`/api/v1/auth`

- `register(RegisterRequest req)`
- `login(LoginRequest req)`
- `refreshToken(RefreshTokenRequest req)`
- `logout(LogoutRequest req)`
- `resetPassword(PasswordResetRequest req)`

Controller 只做：参数校验 + 调用 service + 返回统一信封。

## 4. Service 分层与职责

### 4.1 服务拆分

- `AuthService`
  - 注册、登录、登出、重置密码编排
- `JwtService`
  - Access/Refresh 签发、解析、验签（RS256）
- `RefreshTokenStore`
  - Redis 白名单写入/删除/批量失效
- `PasswordService`
  - 密码哈希与强度校验
- `AuthRiskService`
  - 登录失败计数、限流校验、黑名单校验、验证码挑战判定
- `LoginBlacklistStore`
  - 登录标识黑名单判断与管理
- `LoginAuditService`（可选）
  - 当前阶段允许仅写应用日志

### 4.2 关键事务边界

- 注册：验证码通过并完成 `user` 写入后返回 `RegisterResult`，不自动签发 token。
- 登录：主流程不依赖异步审计；审计失败不影响登录成功。
- 刷新：旧 refresh 失效 + 新 refresh 入白名单必须同请求原子完成。
- 登出：删除当前设备 refresh 为核心动作，不要求实现全设备会话治理。
- 重置密码：更新密码后可选强制失效全部 refresh。

## 5. 五个接口的后端处理步骤（逐步）

## 5.0 当前主流程时序（未注册用户 -> 注册 -> 再登录）

1. 用户第一次进入应用，在登录页输入手机号或账号尝试登录。
2. 前端调用 `POST /api/v1/auth/login`。
3. 后端先校验登录标识格式，只允许“手机号或账号”。
4. 后端检查原始登录标识是否命中 Redis 登录失败封禁阈值或登录黑名单。
5. 如果已命中风控封禁或黑名单，直接拒绝登录。
6. 如果未命中，后端按手机号或账号查询 `users` 表。
7. 若用户不存在，返回 `404 + ACCOUNT_NOT_FOUND`。
8. 前端收到“未注册”结果后，引导用户去注册。
9. 用户在注册页输入手机号，请求验证码。
10. 前端调用 `POST /api/v1/auth/send-code`，并传 `scene=register`。
11. 后端校验手机号格式，并确认手机号尚未注册。
12. 验证码服务检查发送间隔、每日次数限制，然后生成 6 位验证码。
13. 后端将注册验证码写入 Redis，并设置 TTL、尝试次数和已下发标记。
14. 当前开发态会把验证码和过期秒数直接返回前端，便于联调。
15. 用户输入验证码、账号、密码、昵称后提交注册。
16. 前端调用 `POST /api/v1/auth/register`。
17. 后端校验手机号、账号格式，并校验注册验证码。
18. 验证通过后，后端再次检查手机号和账号是否已被占用。
19. 后端用雪花算法生成用户 ID，对密码做哈希，再写入 `users` 表。
20. 注册成功后仅返回 `RegisterResult(nextAction=login)`，不会自动登录，也不会签发 JWT。
21. 用户回到登录页，再次输入“手机号或账号 + 密码”。
22. 前端再次调用 `POST /api/v1/auth/login`。
23. 后端重复执行格式校验、失败次数检查、黑名单检查和用户查询。
24. 若该账号失败次数已达到验证码阈值，后端要求额外提交 `captchaCode`，并校验 `scene=login` 的登录验证码。
25. 密码校验通过后，后端清空该用户相关标识上的失败次数。
26. 后端签发 RSA `accessToken + refreshToken` 双令牌。
27. 后端解析新 `refreshToken` 的 `jti`，将其写入 Redis 白名单。
28. 后端返回 `AuthSessionData(userId, tokens)`，至此登录完成。

## 5.1 注册 `POST /auth/register`

1. 校验 `phone/account/password/nickname/smsCode`。
2. 校验短信验证码有效性（建议来自 mock `send-code`）。
3. 检查手机号和账号是否已存在（唯一索引）。
4. 使用雪花算法生成用户 ID。
5. 密码加密存储（BCrypt）。
6. 创建用户记录。
7. 返回 `RegisterResult(userId, phone, account, nextAction=login, status=registered)`。
8. 不自动签发 Access/Refresh，不自动登录。

失败分支：
- 手机号已注册 -> HTTP 409
- 账号已存在 -> HTTP 409
- 验证码无效/过期 -> HTTP 400
- 限流 -> HTTP 429

## 5.2 登录 `POST /auth/login`

1. 校验 `identifier/password`。
2. 先检查登录失败次数封禁阈值和登录黑名单 `auth:blacklist:login:{identifier}`。
3. 查询用户。
4. 若用户不存在，累计失败次数并返回 `404 + ACCOUNT_NOT_FOUND`，用于前端引导注册。
5. 若用户存在，继续检查该用户关联的手机号和账号是否有任一命中黑名单或封禁阈值。
6. 当失败次数达到阈值后，强制校验 `captchaCode`。
7. 校验密码哈希。
8. 登录失败时累加 `auth:login:failure:{identifier}`（短 TTL）。
9. 登录成功时清理用户相关标识上的失败计数。
10. 签发 Access + Refresh。
11. 保存 refresh 白名单。
12. 记录登录审计日志。
13. 返回 `AuthSessionData`。

失败分支：
- 账号未注册 -> HTTP 404
- 账号或密码错误 -> HTTP 401
- 命中黑名单 -> HTTP 403
- 需要验证码但未提交 -> HTTP 400
- 登录验证码无效/过期/不存在 -> HTTP 400
- 参数错误 -> HTTP 400
- 失败次数达到封禁阈值 -> HTTP 403

## 5.3 刷新 `POST /auth/token/refresh`

1. 验签 refresh token（必须 `token_type=refresh`）。
2. 从 token 提取 `uid/jti`。
3. 若用户当前登录标识命中黑名单，可按策略直接拒绝。
4. 检查 Redis 白名单 `auth:rt:{uid}:{jti}` 是否存在。
5. 不存在则拒绝（可能重放/已注销）。
6. 生成新 Access + 新 Refresh（新 `jti`）。
7. 原子操作：删除旧 `jti` + 写入新 `jti`。
8. 返回 `AuthTokens`。

失败分支：
- token 非法/过期 -> HTTP 401
- 白名单不存在 -> HTTP 401
- 风险控制触发 -> HTTP 401 或 429（按策略）

## 5.4 登出 `POST /auth/logout`

1. 校验 `refreshToken`。
2. 解析 token 拿到 `uid/jti`。
3. 当前阶段默认删除当前 `auth:rt:{uid}:{jti}`。
4. 若保留 `logoutScope` 字段，可先忽略 `all_devices`，统一按当前设备处理。
5. 返回 `ActionResult(success=true, action=logout)`。

幂等要求：重复登出应返回成功，不报错。

## 5.5 重置密码 `POST /auth/password/reset`

1. 校验 `phone/smsCode/newPassword`。
2. 校验短信验证码。
3. 更新密码哈希。
4. 可选：删除用户所有 refresh 白名单（强制重新登录）。
5. 返回 `ActionResult(success=true, action=password_reset)`。

失败分支：
- 验证码无效 -> HTTP 400
- 频次超限 -> HTTP 429

## 6. Redis 设计（认证域）

### 6.1 Key 规范

1. `auth:code:{scene}:{phone}`
- 含义：验证码主键
- 结构：Redis Hash，字段包含 `code/maxAttempts/attempts`
- TTL：验证码有效期，当前默认 10 分钟

2. `auth:code:issued:{scene}:{phone}`
- 含义：验证码已下发标记
- 值：`issued`
- TTL：验证码 TTL + 额外缓冲期，用于区分“已过期”和“从未发送”

3. `auth:code:last:{scene}:{phone}`
- 含义：验证码发送间隔限制键
- 值：`1`
- TTL：发送间隔，当前默认 60 秒

4. `auth:code:count:{scene}:{phone}:{date}`
- 含义：单日验证码发送计数
- 值：发送次数整数
- TTL：1 天

5. `auth:rt:{uid}:{jti}`
- 含义：refresh 白名单项
- 值：`1`
- TTL：与 refresh token 过期时间一致，当前默认 7 天

6. `auth:rt:index:{uid}`
- 含义：某个用户名下所有 refresh token 的索引集合
- 值：Set，成员为各个 refresh token 的 `jti`
- TTL：与该用户最长有效 refresh token 保持一致

7. `auth:login:failure:{identifier}`
- 含义：登录失败计数
- 值：失败次数整数
- TTL：按风控配置，当前默认 30 分钟

8. `auth:blacklist:login:{identifier}`
- 含义：登录黑名单标记
- 值：`1` 或黑名单原因 JSON
- TTL：按风控策略配置，可短期封禁或长期封禁

### 6.2 原子操作要求

- 刷新轮换必须使用 Lua 或事务保证：`DEL oldKey` + `SETEX newKey` 同步完成。
- 黑名单判定必须走独立 key，不要和失败计数共用同一个 Redis 结构。

## 7. 安全策略（最小可用）

- Access TTL：15 分钟；Refresh TTL：7 天。
- RS256 签名，支持 `kid` 多 key 轮换。
- 密码只存哈希，不可逆。
- 登录失败阈值后触发验证码挑战。
- 命中黑名单时拒绝登录，必要时拒绝刷新。
- 短信验证码使用开发态 mock 方案，不强制接真实网关，但保留“生成-存储-校验”完整闭环。

## 8. 错误码矩阵（建议落地）

> HTTP 状态码固定按 OpenAPI；以下业务码用于 `ErrorResponse.code`。

| 场景 | HTTP | 业务码（建议） | message |
| --- | --- | --- | --- |
| 参数校验失败 | 400 | `AUTH_400_VALIDATION` | 参数错误 |
| 账号未注册 | 404 | `AUTH_404_ACCOUNT_NOT_FOUND` | 账号未注册 |
| 注册/重置验证码不存在 | 400 | `AUTH_404_VERIFICATION_NOT_FOUND` | 验证码不存在 |
| 注册/重置验证码过期 | 400 | `AUTH_400_VERIFICATION_EXPIRED` | 验证码已过期 |
| 注册/重置验证码错误 | 400 | `AUTH_400_VERIFICATION_MISMATCH` | 验证码错误 |
| 注册/重置验证码尝试次数过多 | 429 | `AUTH_429_VERIFICATION_ATTEMPTS` | 验证码尝试次数过多 |
| 需要验证码挑战 | 400 | `AUTH_400_CAPTCHA_REQUIRED` | 需要完成人机验证 |
| 验证码挑战失败 | 400 | `AUTH_400_INVALID_CAPTCHA` | 人机验证失败 |
| 未认证/令牌无效 | 401 | `AUTH_401_UNAUTHORIZED` | 认证失败 |
| refresh 白名单缺失 | 401 | `AUTH_401_REFRESH_REPLAY` | 刷新令牌已失效 |
| 命中登录黑名单 | 403 | `AUTH_403_LOGIN_BLOCKED` | 当前账号登录受限 |
| 手机号冲突 | 409 | `AUTH_409_PHONE_EXISTS` | 手机号已注册 |
| 登录/重置限流 | 429 | `AUTH_429_RATE_LIMIT` | 操作过于频繁 |

## 9. 数据库与索引建议

### 9.1 主表（最小）

- `user(id, phone, password_hash, nickname, avatar, status, create_time, update_time)`
- `login_logs(id, user_id, identifier, channel, ip, ua, status, created_at)`（可选）

### 9.2 索引

- `user.phone` 唯一索引
- `login_logs(user_id, created_at)` 普通索引
- `login_logs(identifier, created_at)` 普通索引

## 10. 观测与告警

### 10.1 指标

- `auth_login_total{result}`
- `auth_refresh_total{result}`
- `auth_logout_total{scope}`
- `auth_password_reset_total{result}`
- `auth_token_issue_rt_ms`
- `auth_refresh_rt_ms`

### 10.2 日志字段（至少）

- `requestId`
- `userId`（匿名可空）
- `identifier`
- `ip`
- `ua`
- `channel`
- `action`
- `result`
- `errorCode`

## 11. 测试清单（必须覆盖）

### 11.1 单元测试

- 密码哈希与比对。
- JWT 签发与验签（含过期、错误签名、错误 tokenType）。
- Refresh 白名单存取与 TTL。
- 刷新轮换原子逻辑。

### 11.2 集成测试

- 注册成功、手机号重复失败。
- 登录成功、密码错误失败、限流生效。
- 命中黑名单时登录失败。
- 连续失败达到阈值后必须提交 `captchaCode`。
- `captchaCode` 无效时登录失败。
- 刷新成功、重复刷新失败（重放防护）。
- 命中黑名单后刷新失败。
- 登出后 refresh 立即失效。
- 重置密码后旧密码失效、可选会话全失效。

### 11.3 并发测试

- 同一 refresh 并发刷新，仅一个成功。
- 同账号高并发登录失败计数正确。
- 黑名单写入与并发登录冲突时，最终结果符合“黑名单优先拒绝”。

## 12. 实施顺序（建议 2 个迭代）

### 迭代1（先跑通）

1. 5个接口 + 基础 DTO + JWT RS256。
2. Refresh 白名单 + 刷新轮换。
3. 基础限流（登录/重置）+ 失败计数。
4. 登录黑名单判定。
5. 开发态验证码方案（mock `send-code` + Redis 校验）。

### 迭代2（安全加固）

1. 风控挑战（验证码门槛）。
2. 审计日志与告警。
3. 黑名单治理与后台管理能力（如有后台域）。

## 13. 与现有文档映射

- 上层概述：`后端API-超详细.md` 模块1
- 本文：实现层细化，不改变上层路径与响应契约

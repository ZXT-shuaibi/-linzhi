<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiguang.be.auth.mapper.AuthUserMapper">

    <resultMap id="AuthUserEntityMap" type="com.zhiguang.be.auth.model.AuthUserEntity">
        <constructor>
            <arg column="id" javaType="java.lang.String"/>
            <arg column="phone" javaType="java.lang.String"/>
            <arg column="nickname" javaType="java.lang.String"/>
            <arg column="password_hash" javaType="java.lang.String"/>
            <arg column="role" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <select id="existsByPhone" resultType="boolean">
        select count(1) > 0 from users where phone = #{phone}
    </select>

    <insert id="save">
        insert into users(id, phone, account, nickname, password_hash, role)
        values (#{userId}, #{phone}, #{phone}, #{nickname}, #{passwordHash}, #{role})
    </insert>

    <select id="findByPhone" resultMap="AuthUserEntityMap">
        select id, phone, nickname, password_hash, role from users where phone = #{phone}
    </select>

    <select id="findByUserId" resultMap="AuthUserEntityMap">
        select id, phone, nickname, password_hash, role from users where id = #{userId}
    </select>

    <update id="update">
        update users
           set phone = #{phone},
               nickname = #{nickname},
               password_hash = #{passwordHash},
               updated_at = current_timestamp
         where id = #{userId}
    </update>
</mapper>

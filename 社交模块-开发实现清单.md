# 社交模块开发实现清单（可直接开工）

> 适用范围：邻里知光重构期社交域（模块5）
> 事实来源：`README.md` + `后端API-超详细.md` + `前端API-标准版.md` + `邻里知光-技术分档-详细版.md` + `openapi.yaml`
> 参考实现：本地 `zhiguang` 的 `relation` / `counter` 模块
> 目标：让后端工程师按本清单直接实现 `social` 模块，并能自然衔接 `content` 与后续 `feed`
> 非企业级约束：这是中等复杂度玩具项目，不接真实 Kafka/Canal，不做复杂评论系统，不做大 V 分层缓存，不做超重审计链路
> 特别说明：实现要尽量贴近 `zhiguang` 的职责划分和接口习惯，但不要为了“像企业级”而把文件拆得过细

## 1. 当前范围先收住

本期 `social` 模块只做三件事：

1. 关注 / 取关
2. 点赞 / 取消点赞
3. 收藏 / 取消收藏 + 汇总查询

本期先不做：

- 评论系统
- 真实 Kafka 生产者 / 消费者
- 真实 Canal / Debezium
- 用户关系公开状态接口（如 `following/followedBy/mutual` 的独立 HTTP 接口）
- 大 V Top Cache、SDS 二进制用户计数结构

说明：

- `README.md` 里提到“评论系统”，但当前 `openapi.yaml` 和前后端 API 文档并没有对应评论接口，所以本期不纳入开发主线。
- `zhiguang` 里 `relation` 与 `counter` 分得比较开，`linli` 这里可以在 `social` 域内保留这两个子能力，但代码文件数量要控制，不要拆成一堆细碎小文件。

## 2. 固定接口范围

对齐当前 `openapi.yaml`，社交模块一期固定接口如下：

- `POST /interactions/targets/{targetType}/{targetId}/like`
- `DELETE /interactions/targets/{targetType}/{targetId}/like`
- `POST /interactions/targets/{targetType}/{targetId}/favorite`
- `DELETE /interactions/targets/{targetType}/{targetId}/favorite`
- `GET /interactions/targets/{targetType}/{targetId}/summary`
- `POST /follows/{followeeId}`
- `DELETE /follows/{followeeId}`
- `GET /follows/users/{userId}/following`
- `GET /follows/users/{userId}/followers`

一期内部必须提供但不一定暴露 HTTP 的能力：

- `isFollowing(currentUserId, targetUserId)`：给 `content` 模块判定 `followers` 可见范围
- `getInteractionSummary(targetType, targetId, viewerId)`：给内容详情页直接取互动汇总
- `getInteractionSummaryBatch(targetType, targetIds, viewerId)`：给内容卡片 / feed 批量回填点赞收藏状态

## 2.1 targetType 一期约束

- 文档里写了 `post|merchant`
- 当前 `linli` 已经落地的是内容帖子，因此一期先只真实支持 `post`
- `merchant` 先保留参数位，传入后返回 `400 + BAD_REQUEST`

这样做的原因：

- 先把 `content -> social` 链路闭合
- 不为了文档上的预留能力，提前引入不存在的业务实体

## 3. 数据契约（DTO）

## 3.1 出参 DTO

1. `InteractionActionData`

- `targetType: string`
- `targetId: string`
- `action: string`：`like|favorite`
- `active: boolean`
- `snapshotVersion: integer`

2. `InteractionSummary`

- `targetType: string`
- `targetId: string`
- `likeCount: integer`
- `favoriteCount: integer`
- `viewerLiked: boolean`
- `viewerFavorited: boolean`

3. `FollowActionData`

- `followeeId: string`
- `following: boolean`
- `followerCount: integer`
- `followCount: integer`

4. `FollowUserItem`

- `userId: string`
- `nickname: string`
- `avatar: string`
- `followedAt: datetime`

5. `FollowListData`

- `items: List<FollowUserItem>`
- `page: PageMeta`

## 3.2 DTO 收紧原则

参考你现在对 `content` 模块的要求，DTO 不要拆得太细：

- `InteractionActionData` / `InteractionSummary` 可以放在一个文件里
- `FollowActionData` / `FollowUserItem` / `FollowListData` 可以放在一个文件里
- 不要为了只有几行字段就每个 DTO 单独建一个文件

## 4. 表结构与存储设计

## 4.1 已有表直接复用

当前 `schema.sql` 已经有：

- `following`
- `follower`
- `outbox`

这三张表足够支撑关注链路一期落地。

## 4.2 需要新增的互动记录表

当前 schema 里还没有点赞 / 收藏持久化表，一期建议新增一张轻量互动表：

表名建议：`like_favorite`

建议字段：

- `id BIGINT PRIMARY KEY`
- `user_id BIGINT NOT NULL`
- `target_type VARCHAR(32) NOT NULL`
- `target_id BIGINT NOT NULL`
- `action_type VARCHAR(16) NOT NULL`：`like|favorite`
- `rel_status TINYINT NOT NULL DEFAULT 1`：`1=有效，0=取消`
- `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
- `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`

索引建议：

- 唯一索引：`(user_id, target_type, target_id, action_type)`
- 查询索引：`(target_type, target_id, action_type, rel_status)`

用途说明：

- Redis 位图负责高频状态读写
- `like_favorite` 负责兜底持久化、数据对账、后续重建
- 这样不会只剩 Redis 单点状态

## 4.3 用户表是否加关注数 / 粉丝数

技术文档里提到 `fans_count` / `follow_count`，但当前 `users` 表没有这两个字段。

一期建议：

- 先不往 `users` 表强塞这两个冗余字段
- `FollowActionData` 里的 `followerCount` / `followCount` 从 Redis 计数键或 DB 聚合得到
- 等后面真的有用户主页计数瓶颈，再考虑冗余列

这样更贴合当前项目阶段，不会为了“像大系统”提前加很多同步负担。

## 5. Redis Key 设计

对齐文档语义，但命名尽量明确：

- `like:bit:{targetType}:{targetId}`
- `fav:bit:{targetType}:{targetId}`
- `count:like:{targetType}:{targetId}`
- `count:fav:{targetType}:{targetId}`
- `count:follow:{userId}`
- `count:fans:{userId}`
- `following:zset:{userId}`
- `follower:zset:{userId}`

说明：

- 点赞 / 收藏的“是否已操作”用位图表达
- 点赞 / 收藏总数单独走计数 key
- 关注 / 粉丝列表用 ZSet，member 为用户 ID，score 为关注时间戳
- 不必为了对齐 README 里的 `follow:list:{user_id}` 强行做模糊 key，一期用 `following:zset` / `follower:zset` 更清楚

## 6. Controller / Service / Mapper 建议结构

为了贴近 `zhiguang`，但又不拆得太碎，建议用下面这套结构：

## 6.1 Controller

- `InteractionController`
- `FollowController`

## 6.2 Service

- `InteractionService`
- `FollowService`
- `SocialTargetService` 可选

说明：

- `SocialTargetService` 只在你觉得“校验 target 是否存在”的逻辑开始重复时再抽
- 如果目前只支持 `post`，也可以先把目标校验直接写在 `InteractionServiceImpl` 里

## 6.3 Mapper

建议只保留一个主 mapper：

- `SocialMapper`
- 配一个 `SocialMapper.xml`

在这个 mapper 里统一放：

- `following` 表读写
- `follower` 表读写
- `like_favorite` 表读写
- 跟 `users` 表关联的关注列表查询

这样更符合你前面一直强调的“自然、收紧、别拆太细”。

## 7. 推荐开发顺序

## 7.1 第一阶段：先做关注链路

优先级最高，原因有两个：

1. 当前 `schema.sql` 已经有 `following/follower/outbox`
2. `content` 模块里 `visibility=followers` 会直接依赖 `isFollowing`

这一阶段完成：

- `POST /follows/{followeeId}`
- `DELETE /follows/{followeeId}`
- `GET /follows/users/{userId}/following`
- `GET /follows/users/{userId}/followers`
- 内部方法：`isFollowing(fromUserId, toUserId)`

这一阶段先不追求：

- 真正的 Canal
- 真正的异步 follower 派生消费

一期建议做法：

- 主事务里写 `following` + `outbox`
- 同请求内同步更新 `follower`、Redis 计数、Redis ZSet
- 保留 `outbox` 记录和事件载荷，为以后异步化留接口

这样做的好处：

- 链路先闭合
- 结构上还是贴近 `zhiguang`
- 不会因为没有 MQ / Canal 把业务卡住

## 7.2 第二阶段：做点赞收藏链路

这一阶段完成：

- 点赞 / 取消点赞
- 收藏 / 取消收藏
- 汇总查询
- 内部批量汇总方法

关键要求：

- 必须用 Lua 保证“状态切换 + 计数增减”原子完成
- 必须把最终状态落到 `like_favorite` 表
- 重复点赞不能重复加 1
- 取消未点赞状态不能减成负数

## 7.3 第三阶段：反向补 content 模块

这一阶段不是新接口，而是把 `social` 真接进 `content`：

- `PostDetail.likeCount`
- `PostDetail.favoriteCount`
- `PostDetail.liked`
- `PostDetail.faved`
- `PostCard` 对应字段
- `followers` 可见范围改成真实调用 `FollowService.isFollowing`

这一步做完，`content` 才算真正从“占位社交字段”进入“真实社交字段”。

## 7.4 第四阶段：补事件出口

这一阶段只做“留口”，不做真消息中间件：

- 定义 `CounterEventPayload`
- 定义 `FollowEventPayload`
- follow 操作写 `outbox`
- interaction 操作至少保留本地事件对象或可扩展发布接口

目的是后续做 `feed` 时不返工。

## 8. 接口时序清单

## 8.1 关注用户

1. 登录态校验，通过 JWT 取当前用户 ID
2. 校验 `followeeId` 合法且目标用户存在
3. 拒绝自己关注自己
4. 查询当前是否已关注
5. 若已关注，返回 `409`
6. 生成关系 ID，写入 `following`
7. 同事务写入 `outbox`
8. 同步更新 `follower`
9. 同步更新 Redis：
- `count:follow:{fromUserId}`
- `count:fans:{followeeId}`
- `following:zset:{fromUserId}`
- `follower:zset:{followeeId}`
10. 返回 `FollowActionData`

## 8.2 取消关注

1. 登录态校验
2. 校验目标用户存在
3. 查 `following` 当前关系
4. 若不存在有效关系，直接返回“已取消”状态或按幂等成功处理
5. 更新 `following.rel_status=0`
6. 同事务写 `outbox`
7. 同步更新 `follower.rel_status=0` 或删除
8. 同步更新 Redis 计数和 ZSet
9. 返回 `FollowActionData`

建议：

- 对 `DELETE /follows/{followeeId}` 走幂等成功，不要把“本来就没关注”也做成硬错误

## 8.3 点赞 / 收藏

1. 登录态校验
2. 校验 `targetType=post`
3. 校验 `targetId` 对应帖子存在且允许互动
4. 执行 Lua：
- 判断当前位图状态
- 未操作时置位并计数 `+1`
- 已操作时不重复加
- 返回“是否发生状态变化 + 最新计数”
5. Upsert `like_favorite`
6. 生成 `snapshotVersion`
7. 返回 `InteractionActionData`

## 8.4 取消点赞 / 取消收藏

1. 登录态校验
2. 校验目标存在
3. 执行 Lua：
- 已操作时清位并计数 `-1`
- 未操作时不重复减
- 计数不得小于 0
4. Update `like_favorite.rel_status=0`
5. 返回 `InteractionActionData`

## 8.5 查询互动汇总

1. 校验目标存在
2. 读取 Redis 计数：
- `count:like`
- `count:fav`
3. 若用户已登录，再读位图状态：
- `viewerLiked`
- `viewerFavorited`
4. 组装 `InteractionSummary`
5. Redis 异常时允许降级到 DB 快照表统计

## 8.6 查询关注 / 粉丝列表

1. 校验 `userId` 存在
2. 优先读 Redis ZSet 分页
3. 若缓存缺失，则查 DB
4. 回填 Redis ZSet
5. 批量查用户基本信息
6. 返回 `FollowListData`

一期分页建议：

- 先用 `page/size`
- 不急着上游标分页
- 这样和当前 `openapi.yaml` 更一致

## 9. 状态机清单

## 9.1 关注关系状态机

状态：

- `NONE`
- `FOLLOWING`
- `UNFOLLOWED`

流转：

- `NONE -> FOLLOWING`
- `FOLLOWING -> UNFOLLOWED`
- `UNFOLLOWED -> FOLLOWING`

落库策略：

- `following` / `follower` 不建议物理删记录
- 用 `rel_status` 保留历史更稳
- 重新关注时直接恢复为 `1`

## 9.2 点赞 / 收藏状态机

点赞与收藏各自独立：

- `INACTIVE`
- `ACTIVE`

流转：

- `INACTIVE -> ACTIVE`
- `ACTIVE -> INACTIVE`

一致性要求：

- Redis 位图状态与 `like_favorite.rel_status` 最终一致
- Redis 计数与位图切换必须同一 Lua 脚本完成

## 10. 事件流清单

## 10.1 follow-event

事件类型：

- `FOLLOW_CREATED`
- `FOLLOW_REMOVED`

建议载荷：

- `eventId`
- `eventType`
- `followerId`
- `followeeId`
- `occurredAt`

一期写法：

- 先写 `outbox`
- 当前服务内同步完成派生更新
- 后期如果真要接 Kafka / Canal，只需要把 outbox 往外桥接

## 10.2 counter-event

事件类型：

- `LIKE_CHANGED`
- `FAVORITE_CHANGED`

建议载荷：

- `eventId`
- `eventType`
- `targetType`
- `targetId`
- `action`
- `operatorId`
- `occurredAt`

一期用途：

- 先作为 `feed` / `content` 局部缓存刷新预留
- 可以先不落真实 MQ

## 11. 与 zhiguang 对齐的点

必须对齐：

- 关注链路要有 `following` 主表思路
- 要有 `outbox` 事件出口
- 关注列表 / 粉丝列表服务化
- 点赞收藏要区分“全局计数”和“用户维度状态”
- `content` / `feed` 中的 `liked` / `faved` 应在返回时叠加，不要污染公共缓存

不必硬对齐：

- `zhiguang` 的大 V 本地缓存
- `zhiguang` 的 SDS 用户计数
- `zhiguang` 的真实 MQ / 伪从消费
- 偏移分页 + 游标分页双支持

`linli` 一期以“逻辑闭环 + 结构不返工”为准。

## 12. 需要同步修改的现有模块

1. `content`

- 详情页改为真实取互动汇总
- 卡片列表改为批量回填互动状态
- `followers` 可见性改为真实依赖 `FollowService.isFollowing`

2. `openapi.yaml`

- 如果接口名、字段名与代码不一致，需要以当前 `openapi` 为准回调代码
- 目前不需要新增外部接口，只需要把 `x-maturity` 从“文档已写”变成“代码已实现”

3. `schema.sql`

- 增加 `like_favorite`
- 保持已有 `following/follower/outbox` 不回退

## 13. 实现清单（按顺序勾）

- [ ] 新增 `like_favorite` 表和索引
- [ ] 新建 `social` 域下的 Controller / Service / Mapper 骨架
- [ ] 先实现 `FollowService`
- [ ] 打通关注 / 取关 / 列表 / `isFollowing`
- [ ] Redis 关注计数与 ZSet 缓存接入
- [ ] `outbox` 写入 follow 事件
- [ ] 再实现 `InteractionService`
- [ ] Lua 完成点赞 / 收藏原子切换
- [ ] `like_favorite` 持久化落库
- [ ] 打通汇总查询
- [ ] 给 `content` 模块回填真实互动字段
- [ ] 给 `content` 模块接入真实 followers 可见性校验
- [ ] 预留 `counter-event` / `follow-event` 载荷对象

## 14. 验收标准

- 重复关注不会产生重复关系
- 取消关注后关注数 / 粉丝数能同步变化
- 重复点赞不会重复加 1
- 取消未点赞状态不会减成负数
- 内容详情里的点赞 / 收藏 / liked / faved 变为真实值
- `followers` 可见内容不再是占位判断
- 整个社交模块不依赖真实 Kafka / Canal 也能闭环跑通

## 15. 一句话施工建议

先做关注链路，再做点赞收藏，再反补 `content`，最后留事件口子给 `feed`。  
这样最贴近你现在项目的节奏，也最符合“参考 `zhiguang`，但不过度企业级”的目标。

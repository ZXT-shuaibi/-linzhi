# 社交计数体系-实现路线图

> 适用范围：`linli` 当前 `social + content + feed` 联动阶段  
> 参考来源：`zhiguang` 的 `relation / counter` 思路，以及现有《社交模块-开发实现清单》  
> 当前原则：先把 Kafka 之前的同步闭环做扎实，再预留后续异步聚合与灾难回放演进点

## 1. 当前目标

这条线的核心不是“把所有企业级基础设施一次性堆满”，而是先把下面三件事做闭环：

1. 用户操作后，前台立刻能看到真实状态。
2. 内容详情、内容卡片、Feed 卡片都能拿到真实互动数据。
3. 即使 Redis 计数视图漂了，也能从事实层重新拉回来。

当前我们把社交计数拆成四层：

1. 事实层：`like_favorite`、`following`、`follower`
2. 用户态快速读取层：分片位图 `bm:*`
3. 实体/用户计数视图层：`cnt:*`、`ucnt:*`
4. 后续演进层：`agg:*`、Kafka、灾难回放

## 2. 当前已实现

### 2.1 关注链路

已经具备：

1. 关注 / 取关
2. 关注列表 / 粉丝列表
3. `isFollowing(...)`
4. 用户维计数 `ucnt:{userId}` 的 `followings / followers`

### 2.2 互动链路

已经具备：

1. 点赞 / 取消点赞
2. 收藏 / 取消收藏
3. 互动事实表 `like_favorite`
4. 分片位图 `bm:like:*`、`bm:fav:*`
5. 实体计数视图 `cnt:s1:{targetType}:{targetId}`
6. 作者维计数 `ucnt:{authorId}` 的 `likedPosts / favedPosts`

### 2.3 与内容和 Feed 的联动

已经具备：

1. `content` 详情页真实回填 `likeCount / favoriteCount / liked / faved`
2. `content` 列表和 `feed` 批量回填互动状态
3. `followers` 可见内容改为真实走 `FollowService.isFollowing(...)`
4. 内容发布 / 删除联动作者 `ucnt.posts`

## 3. 当前还差的关键点

Kafka 之前，最值得先补的是这三步：

1. `cnt:*` 缺失或结构异常时，优先从位图事实层重建，而不是直接回 DB 聚合。
2. 帖子维计数重建要加轻量重建锁，避免并发重复重建。
3. 保留 DB 聚合作为最后兜底，避免单次重建失败时接口直接不可用。

这一步做完后，`social` 的计数读链就会变成：

1. 优先读 `cnt:*`
2. 缺失时尝试从 `bm:*` 做 `BITCOUNT` 重建
3. 仍失败时再回表聚合

## 4. 一次完整互动流程

以“用户点赞帖子”为例：

1. 校验登录态
2. 校验目标帖子存在、已发布、当前查看者有权限互动
3. 切换位图事实层状态 `bm:like:{targetType}:{targetId}:{chunk}`
4. 持久化事实表 `like_favorite`
5. 写 `outbox`
6. 同步折叠实体计数 `cnt:*`
7. 同步折叠作者计数 `ucnt:*`
8. 返回动作结果给前端

读路径则分成两部分：

1. “我有没有点过赞/收藏”优先查位图
2. “这篇帖子现在多少赞/收藏”优先查 `cnt:*`

## 5. Kafka 之前的实现口径

当前阶段我们明确采用下面这套口径：

1. 事实层以 DB 为权威来源。
2. Redis 里的 `bm:* / cnt:* / ucnt:*` 都是高性能近实时视图。
3. `cnt:*` 允许短暂漂移，但要具备按需重建能力。
4. 暂时不接真实 Kafka，不做真正的 `agg:*` 聚合桶消费链。
5. 暂时不做完整灾难回放，只保留事件出口和演进接口。

## 6. 后续演进顺序

### 6.1 下一步

下一步优先做：

1. 帖子维计数按位图重建
2. 重建锁
3. 读链优先重建、最后再 DB 兜底

### 6.2 再下一步

再往后做：

1. `agg:*` 本地聚合桶
2. 定时折叠进 `cnt:*`
3. 用户维和帖子维的轻量对账

### 6.3 最后再做

最后才做：

1. Kafka `counter-event`
2. Kafka 消费聚合
3. 灾难回放
4. 更完整的计数对账与自动修复

## 7. 设计边界

为了保持项目自然、不过度企业级，当前阶段不做这些：

1. 不强行拆独立 `counter` 模块
2. 不提前引入一整套 MQ 基础设施
3. 不为了演进路径把代码切得特别碎
4. 不把所有计数问题都升级成平台级治理问题

当前最重要的是：链路闭环、接口稳定、结构别返工。

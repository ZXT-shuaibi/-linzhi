# 内容模块-开发实现清单
> 参考基线：`zhiguang` 的 `knowpost + storage`
> 当前目标：先在 `linli` 中落一条“草稿 -> 预签名 -> 确认正文 -> 更新元数据 -> 发布 -> 同步 Discover -> 查询详情/feed/mine”的最小可用闭环

## 1. 接口时序清单

### 1.1 创建草稿
- 接口：`POST /api/v1/posts/drafts`
- 鉴权：需要登录
- 输入：无请求体
- 输出：`DraftData(postId, status, createdAt)`
- 后端步骤：
1. 从访问令牌读取当前用户 `userId`
2. 使用雪花算法生成 `postId`
3. 向 `know_posts` 写入一条 `draft` 状态记录
4. 返回草稿主键和初始状态

### 1.2 申请上传预签名
- 接口：`POST /api/v1/storage/presign`
- 鉴权：需要登录
- 输入：`StoragePresignRequest(postId, filename, contentType, purpose)`
- 输出：`StoragePresignData(uploadUrl, objectKey, expireAt)`
- 当前约束：
  - `purpose` 仅支持 `content|image`
  - 封面不再单独建模，统一走图片资源
- 后端步骤：
1. 校验 `postId` 是否存在且归属当前用户
2. 根据 `purpose` 生成对象路径
3. 生成开发态 mock 预签名地址
4. 返回 `objectKey` 与过期时间

### 1.3 确认正文上传
- 接口：`POST /api/v1/posts/{postId}/content/confirm`
- 鉴权：需要登录
- 输入：`ConfirmContentRequest(objectKey, etag, sha256, size)`
- 输出：空响应
- 后端步骤：
1. 校验草稿存在且归属当前用户
2. 校验 `objectKey` 前缀属于当前 `postId`
3. 生成公开 `contentUrl`
4. 保存正文对象信息与校验信息
5. 状态推进到 `content_confirmed` 或 `metadata_completed`

### 1.4 更新元数据
- 接口：`PUT /api/v1/posts/{postId}/metadata`
- 兼容接口：`PATCH /api/v1/posts/{postId}`
- 鉴权：需要登录
- 输入：`UpdatePostMetadataRequest`
- 输出：`PostDetail`
- 后端步骤：
1. 校验草稿存在且归属当前用户
2. 合并标题、摘要、标签、图片列表、位置、可见性、置顶状态
3. 若正文已确认且标题已具备，则状态推进到 `metadata_completed`
4. 返回最新详情

### 1.5 发布内容
- 接口：`POST /api/v1/posts/{postId}/publish`
- 鉴权：需要登录
- 输入：无请求体
- 输出：`PostDetail`
- 后端步骤：
1. 校验草稿存在且归属当前用户
2. 校验发布前置条件：至少已存在正文和标题
3. 更新 `visible/status/publish_time`
4. 同事务写入 `outbox`
5. 同步维护 Discover 索引
6. 返回发布后的详情

### 1.6 查询详情
- 接口：`GET /api/v1/posts/{postId}`
- 鉴权：公开可访问；若携带令牌则按登录视角补充权限判断
- 输出：`PostDetail`
- 访问规则：
  - `published + public`：匿名可看
  - `published + followers`：作者本人或关注作者的用户可看
  - 其他情况：仅作者本人可看

### 1.7 更新置顶状态
- 接口：`PATCH /api/v1/posts/{postId}/top?isTop=true|false`
- 鉴权：需要登录
- 输出：`PostDetail`
- 后端步骤：
1. 校验文章存在且归属当前用户
2. 仅允许对已发布文章调整置顶
3. 更新 `is_top`
4. 返回最新详情

### 1.8 更新可见性
- 接口：`PATCH /api/v1/posts/{postId}/visibility?visibility=public|followers|private`
- 鉴权：需要登录
- 输出：`PostDetail`
- 后端步骤：
1. 校验文章存在且归属当前用户
2. 仅允许对已发布文章调整可见性
3. 更新 `visible`
4. 同步写入 `outbox`
5. 同步维护 Discover 索引
6. 返回最新详情

### 1.9 删除内容
- 接口：`DELETE /api/v1/posts/{postId}`
- 鉴权：需要登录
- 输出：空响应
- 后端步骤：
1. 校验文章存在且归属当前用户
2. 执行软删除，状态更新为 `deleted`
3. 同步写入 `outbox`
4. 从 Discover 移除

### 1.10 公开内容流
- 接口：`GET /api/v1/posts/feed`
- 鉴权：匿名可访问；若携带令牌则补充用户维度字段
- 输出：`PostPageData`
- 当前卡片字段：
  - `postId/title/summary/coverUrl/tags/author/visibility/isTop/publishedAt`
  - `likeCount/favoriteCount/liked/faved`
- 当前实现说明：
  - 结构已向 `zhiguang` 对齐
  - 互动字段当前阶段先以默认值返回，后续再接互动模块

### 1.11 我的已发布内容
- 接口：`GET /api/v1/posts/mine`
- 鉴权：需要登录
- 输出：`PostPageData`
- 后端步骤：
1. 读取当前用户 `userId`
2. 仅查询该用户自己的 `published` 内容
3. 按 `is_top desc, publish_time desc` 排序
4. 返回卡片列表和 `hasMore`

## 2. 状态机清单

- `draft`
  - 含义：草稿已创建，但正文对象和元数据还未补齐
  - 来源：`createDraft`

- `content_confirmed`
  - 含义：正文已完成上传确认，已保存 `objectKey/etag/sha256/size/contentUrl`
  - 来源：`confirmContent`

- `metadata_completed`
  - 含义：标题、摘要、标签、图片、位置等关键元数据已具备
  - 来源：`updateMetadata`

- `published`
  - 含义：内容已发布，对应可见性开始生效
  - 来源：`publish`

- `deleted`
  - 含义：软删除状态，对外详情统一按不存在处理

### 合法流转
- `draft -> content_confirmed`
- `draft -> metadata_completed`
- `content_confirmed -> metadata_completed`
- `draft/content_confirmed/metadata_completed -> published`
- `published -> deleted`

### 发布前最小校验
- `contentUrl` 不为空
- `contentObjectKey` 不为空
- `title` 不为空
- 内容归属当前用户

## 3. 事件流清单

### 3.1 post-published
- 事件类型：`POST_PUBLISHED`
- 触发时机：发布成功后
- 当前实现：
  - 发布成功后同步更新 Discover
  - 同时保留 `outbox` 记录作为后续扩展入口

### 3.2 post-visibility-changed
- 事件类型：`POST_VISIBILITY_CHANGED`
- 触发时机：已发布内容修改可见性后
- 当前实现：
  - 变更后立即同步 Discover
  - 同时写入 `outbox` 便于后续补偿

### 3.3 post-deleted
- 事件类型：`POST_DELETED`
- 触发时机：内容删除后
- 当前实现：
  - 删除后立即从 Discover 移除
  - 同时写入 `outbox`

### 3.4 下游消费方向
- Discover：当前已在发布、改可见性、删除时同步维护 Geo 索引
- Search：后续可消费 `post-published`
- RAG：后续可消费 `post-published`
- Feed：后续可做局部缓存失效或刷新

## 4. 当前阶段边界

- 当前阶段已经打通 `content -> discover` 的同步闭环
- 当前阶段已经补上公开 `feed` 和“我的已发布”两条基础读链
- 当前阶段 `followers` 可见性已经在详情访问控制中真正生效
- 当前阶段 feed 卡片结构已补齐到更接近 `zhiguang`，但互动数据仍为占位值
- 当前阶段保留 `outbox`，但不要求本轮把 Kafka、搜索消费者、RAG 消费者全部接齐

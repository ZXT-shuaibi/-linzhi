# 内容模块-开发实现清单

> 参考基线：`zhiguang` 的 `knowpost + storage` 实现  
> 当前目标：先在 `linli` 中落一条“草稿 -> 预签名 -> 确认内容 -> 更新元数据 -> 发布 -> 写 outbox”的最小可用主链路

## 1. 接口时序清单

### 1.1 创建草稿

- 接口：`POST /api/v1/posts/drafts`
- 鉴权：需要登录
- 输入：`CreateDraftRequest`
- 输出：`DraftData(postId, status, createdAt)`
- 后端步骤：
  1. 从访问令牌读取当前用户 `userId`
  2. 使用雪花算法生成 `postId`
  3. 向 `know_posts` 写入一条 `draft` 状态记录
  4. 返回草稿主键和初始状态

### 1.2 申请上传预签名

- 接口：`POST /api/v1/storage/presign`
- 鉴权：需要登录
- 输入：`StoragePresignRequest(postId, filename, contentType, purpose)`
- 输出：`StoragePresignData(uploadUrl, objectKey, expireAt)`
- 后端步骤：
  1. 校验 `postId` 是否存在且归属当前用户
  2. 根据 `purpose=content|cover|image` 生成对象路径
  3. 生成开发态 mock 预签名地址
  4. 返回 `objectKey` 与过期时间

### 1.3 确认内容上传

- 接口：`POST /api/v1/posts/{postId}/content/confirm`
- 鉴权：需要登录
- 输入：`ConfirmContentRequest(objectKey, etag, sha256, size)`
- 输出：`ConfirmContentData(postId, status, objectKey)`
- 后端步骤：
  1. 校验草稿存在且归属当前用户
  2. 校验 `objectKey` 前缀属于该 `postId`
  3. 生成可访问的 `contentUrl`
  4. 更新正文对象信息与内容校验信息
  5. 状态推进到 `content_confirmed`

### 1.4 更新元数据

- 接口：`PUT /api/v1/posts/{postId}/metadata`
- 兼容接口：`PATCH /api/v1/posts/{postId}`
- 鉴权：需要登录
- 输入：`UpdatePostMetadataRequest`
- 输出：`PostDetail`
- 后端步骤：
  1. 校验草稿存在且归属当前用户
  2. 合并标题、摘要、标签、图片列表、封面、位置、可见性、置顶状态等字段
  3. 若正文已确认且标题等关键元数据已具备，则状态推进到 `metadata_completed`
  4. 返回最新详情

### 1.5 发布内容

- 接口：`POST /api/v1/posts/{postId}/publish`
- 鉴权：需要登录
- 输入：`PublishPostRequest(visibility, publishAt)`
- 输出：`PostDetail`
- 后端步骤：
  1. 校验草稿存在且归属当前用户
  2. 校验发布前置条件：至少已有正文对象和标题
  3. 更新 `visible/status/publish_time`
  4. 同事务写入 `outbox`
  5. 返回发布后的详情

### 1.6 查询详情

- 接口：`GET /api/v1/posts/{postId}`
- 鉴权：公开可访问，携带令牌时补充作者视角权限
- 输出：`PostDetail`
- 后端步骤：
  1. 查询 `know_posts + users`
  2. 若为 `published + public`，允许匿名查看
  3. 若为非公开内容，仅作者本人可见
  4. 返回内容详情

### 1.7 更新置顶状态

- 接口：`PATCH /api/v1/posts/{postId}/top`
- 鉴权：需要登录
- 输入：`UpdateTopRequest`
- 输出：`PostDetail`
- 后端步骤：
  1. 校验文章存在且归属当前用户
  2. 仅允许对已发布文章调整置顶状态
  3. 更新 `is_top`
  4. 返回最新详情

### 1.8 更新可见性

- 接口：`PATCH /api/v1/posts/{postId}/visibility`
- 鉴权：需要登录
- 输入：`UpdateVisibilityRequest`
- 输出：`PostDetail`
- 后端步骤：
  1. 校验文章存在且归属当前用户
  2. 仅允许对已发布文章调整可见性
  3. 更新 `visible`
  4. 返回最新详情

### 1.9 删除内容

- 接口：`DELETE /api/v1/posts/{postId}`
- 鉴权：需要登录
- 输出：空响应
- 后端步骤：
  1. 校验文章存在且归属当前用户
  2. 执行软删除，状态更新为 `deleted`
  3. 后续详情查询统一按不存在处理

## 2. 状态机清单

- `draft`
  - 含义：草稿已创建，但正文对象和元数据都还未补齐
  - 来源：`createDraft`

- `content_confirmed`
  - 含义：正文已完成上传确认，已保存 `objectKey/etag/sha256/size/contentUrl`
  - 来源：`confirmContent`

- `metadata_completed`
  - 含义：标题、摘要、标签、封面、位置等关键元数据已具备，可进入发布前校验
  - 来源：`updateMetadata`

- `published`
  - 含义：内容已发布，对应可见范围开始生效
  - 来源：`publish`

- `deleted`
  - 含义：软删除状态，对外详情统一按不存在处理

### 合法流转

- `draft -> content_confirmed`
- `draft -> metadata_completed`
- `content_confirmed -> metadata_completed`
- `draft/content_confirmed/metadata_completed -> published`
- `published -> deleted`

### 发布前最小校验

- `contentUrl` 不为空
- `contentObjectKey` 不为空
- `title` 不为空
- 内容归属当前用户

## 3. 事件流清单

### 3.1 post-published

- 事件名：`post-published`
- 事件类型：`POST_PUBLISHED`
- 触发时机：发布成功后
- 投递方式：先写 `outbox`，后续由一致性链路异步投递
- 载荷建议：
  - `eventId`
  - `eventType`
  - `postId`
  - `authorId`
  - `visibility`
  - `location`
  - `occurredAt`

### 3.2 下游消费方向

- 搜索模块：构建或更新检索索引
- RAG 模块：构建或刷新知识分块索引
- Feed 模块：做页面失效或局部刷新
- Discover 模块：后续把位置信息写入 Geo 索引

### 3.3 当前阶段实现边界

- 当前阶段只要求把 `POST_PUBLISHED` 可靠写入 `outbox`
- 不要求本轮把 Kafka、搜索消费者、RAG 消费者全部补齐
- 事件字段命名统一使用 `post-published`，不再混用 `content_published`
